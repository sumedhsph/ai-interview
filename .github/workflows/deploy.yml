# .github/workflows/deploy.yml
name: Test → Build → Deploy

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run Lint
        run: npm run lint
        continue-on-error: false

      - name: Run Tests (Vitest) - MUST PASS
        run: npm run test
        continue-on-error: false

      - name: Build Next.js
        run: npm run build

      - name: Deploy to Vercel (Only if tests pass)
        uses: amondnet/vercel-action@v25  # ← v20 se v25 pe upgrade kiya (latest stable)
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}  # Yeh confirm — secrets mein VERCEL_TOKEN hona chahiye
          vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
          vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
          vercel-args: '--prod --confirm'  # ← --confirm add kiya (CLI hang na ho)
          scope: ${{ secrets.VERCEL_ORG_ID }}  # Optional, but safe